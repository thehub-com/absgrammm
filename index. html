<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Absgram</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            background: #0a0f18;
            height: 100vh;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        
        /* ЗАСТАВКА */
        .splash-container {
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            position: relative;
        }
        
        /* АНИМАЦИОННЫЙ КОНТЕЙНЕР */
        .animation-box {
            width: 280px;
            height: 150px;
            position: relative;
            margin-bottom: 100px;
        }
        
        /* САМОЛЕТИК */
        .plane {
            position: absolute;
            width: 60px;
            height: 60px;
            opacity: 0;
            z-index: 10;
            animation: planeFly 1.8s ease-out forwards;
        }
        
        @keyframes planeFly {
            0% {
                transform: translateX(-140px) translateY(0) rotate(0deg);
                opacity: 0;
            }
            20% {
                opacity: 1;
            }
            70% {
                transform: translateX(0) translateY(0) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateX(140px) translateY(0) rotate(0deg);
                opacity: 0;
            }
        }
        
        .plane svg {
            width: 100%;
            height: 100%;
            fill: #FF9800;
            filter: drop-shadow(0 0 15px rgba(255, 152, 0, 0.8));
        }
        
        /* ТЕКСТ ABS */
        .abs-text {
            position: absolute;
            left: 0;
            top: 50%;
            transform: translateY(-50%);
            font-family: 'Arial Black', sans-serif;
            font-size: 70px;
            font-weight: 900;
            background: linear-gradient(45deg, #FFD700, #FFFFFF);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            opacity: 0;
            animation: absAppear 0.8s 1.5s ease-out forwards;
            text-shadow: 0 0 30px rgba(255, 215, 0, 0.5);
            letter-spacing: 2px;
        }
        
        @keyframes absAppear {
            0% {
                opacity: 0;
                transform: translateY(-50%) scale(0.7);
                filter: blur(10px);
            }
            100% {
                opacity: 1;
                transform: translateY(-50%) scale(1);
                filter: blur(0);
            }
        }
        
        /* ТЕКСТ COMPANY */
        .company-text {
            position: absolute;
            right: -10px;
            top: 50%;
            transform: translateY(-40%);
            font-family: 'Arial', sans-serif;
            font-size: 28px;
            font-weight: 600;
            background: linear-gradient(45deg, #FFFFFF, #FFB74D);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            opacity: 0;
            animation: companyAppear 0.6s 2s ease-out forwards;
            letter-spacing: 1px;
        }
        
        @keyframes companyAppear {
            0% {
                opacity: 0;
                transform: translateY(-40%) translateX(-20px);
                filter: blur(5px);
            }
            100% {
                opacity: 0.9;
                transform: translateY(-40%) translateX(0);
                filter: blur(0);
            }
        }
        
        /* НИЖНИЙ ТЕКСТ */
        .bottom-content {
            text-align: center;
            padding: 0 20px;
            opacity: 0;
            animation: fadeInUp 0.6s 2.5s ease-out forwards;
        }
        
        .support-text {
            font-family: 'Arial', sans-serif;
            font-size: 18px;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 20px;
            line-height: 1.4;
        }
        
        .website-link {
            display: inline-block;
            color: #FF9800;
            text-decoration: none;
            font-size: 16px;
            padding: 12px 24px;
            border: 1px solid rgba(255, 152, 0, 0.5);
            border-radius: 25px;
            background: rgba(255, 152, 0, 0.1);
            backdrop-filter: blur(10px);
            transition: all 0.3s;
            margin-bottom: 30px;
        }
        
        .website-link:hover {
            background: #FF9800;
            color: #000;
            transform: translateY(-2px);
        }
        
        /* ПОЛОСА ЗАГРУЗКИ */
        .loading-bar {
            width: 250px;
            height: 4px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 2px;
            overflow: hidden;
        }
        
        .loading-progress {
            width: 0%;
            height: 100%;
            background: linear-gradient(90deg, #FF9800, #FF5722);
            animation: loading 3s 2.8s ease-in-out forwards;
        }
        
        @keyframes loading {
            0% { width: 0%; }
            100% { width: 100%; }
        }
        
        @keyframes fadeInUp {
            0% {
                opacity: 0;
                transform: translateY(20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="splash-container">
        <div class="animation-box">
            <!-- Самолетик -->
            <div class="plane">
                <svg viewBox="0 0 24 24">
                    <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
                </svg>
            </div>
            
            <!-- Буквы ABS -->
            <div class="abs-text">ABS</div>
            
            <!-- Слово company -->
            <div class="company-text">company</div>
        </div>
        
        <div class="bottom-content">
            <div class="support-text">
                Нашли проблему? Напишите нам<br>
                Мы всегда готовы помочь
            </div>
            
            <a href="http://supportabs.tilda.ws/" class="website-link" target="_blank">
                abs-company.com
            </a>
            
            <div class="loading-bar">
                <div class="loading-progress"></div>
            </div>
        </div>
    </div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    console.log('ABScompany заставка загружена');
    
    // Автопереход через 6 секунд
    setTimeout(function() {
        console.log('Заставка завершена');
        // Здесь будет переход на следующий экран
        // window.location.href = 'auth.html';
    }, 6000);
});
</script>
</body>
</html>
